info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/identity/users
  body:
    type: form-urlencoded
    data:
      - name: email
        value: testing@gmail.com
      - name: password
        value: Testing123@
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
