info:
  name: Get All Users
  type: http

http:
  method: GET
  url: http://localhost:3000/identity/all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
