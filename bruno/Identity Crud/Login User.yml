info:
  name: Login User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/identity/login?email
  params:
    - name: email
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
